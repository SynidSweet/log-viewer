# NextAuth.js Configuration
# Required for Google authentication
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# NextAuth.js Secret - Required for JWT encryption
# Generate a good secret with: openssl rand -base64 32
NEXTAUTH_SECRET=your-nextauth-secret

# Base URL of your deployed application (required for callbacks)
NEXTAUTH_URL=http://localhost:3000

# Access Restriction (Optional)
# Comma-separated list of allowed email addresses
ALLOWED_EMAILS=user1@example.com,user2@example.com

# Comma-separated list of allowed email domains
ALLOWED_DOMAINS=company.com,myorg.org 